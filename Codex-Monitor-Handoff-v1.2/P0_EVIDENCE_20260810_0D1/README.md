# Phase 0D.1 sanitized evidence

This directory contains the standalone-CLI remediation and P0 revalidation evidence captured on 2026-08-10.

- The OpenAI standalone installer was run only after explicit user approval. It installed standalone Codex CLI `0.147.0`; PATH resolves to the standalone CLI.
- The managed `remote-control` start attempt created the official control socket. Its external remote-control connection reported an error, but the local socket existed with owner-only permissions and accepted a local WebSocket client.
- The schema archive was generated by the installed standalone CLI without `--experimental`.
- Files under `sanitized/` contain only protocol method names, success/error booleans, and response field names. No raw JSON-RPC payload, credential, account value, thread/turn/item identity, prompt, message, command, or filesystem path is retained.
- `desktop_turn_observer*.json` records the passive observation windows. They contain no Desktop-created Turn, item-lifecycle, approval, or terminal Turn event. This is negative evidence, not a substitute for the required observer proof.
- No independent app-server was started. The Probe did not create, resume, interrupt, subscribe to, alter, or approve a Turn.

See `../P0_REPORT_REVALIDATION_V2_DRAFT.md` and `../PHASE_0D1_REPORT.md` for the decision.
