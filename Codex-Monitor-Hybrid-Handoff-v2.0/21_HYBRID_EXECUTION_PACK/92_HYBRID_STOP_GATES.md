# Hybrid Stop Gates

Stop immediately if implementation requires:

```text
Desktop thread/resume/start/fork to manufacture observation
private backend
credential extraction
screen/accessibility scraping
continuous JSONL as runtime truth
guessing approval/quota/cost/Session Token
mock fixture promotion to real capability
reset-credit mutation without explicit user authorization
public/beta claim while release maturity remains Internal-only
```

Report the blocker. Do not route around it.
