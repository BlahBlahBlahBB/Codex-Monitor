# H3 — Terra / High — Capability-driven Domain & State Engine

> Execute only if H2R authorizes H3.

```text
实现：
- AccountAvailability
- RuntimeSourceAvailability
- ObservationMode
- CapabilitySnapshot
- SnapshotSummary
- RuntimeState
- runtimeUnavailable
- live-source eligibility
- existing frozen live priority/timers
- capability loss atomic clearing
- Desktop snapshot bypass
- source-scoped aggregation
- forbidden inference tests

H3 可用 fixtures 测试 WAITING_APPROVAL/FAILED/INTERRUPTED 的 Domain 语义，
但真实 Adapter capability 仍必须保持 unvalidated，产品不能启用。

Current activity 只能建立 sanitization/domain contract，不得把 unvalidated Adapter 文本显示给用户。

输出 PHASE_H3_REPORT.md，停止。
下一阶段建议 Sol High H3R。
```
