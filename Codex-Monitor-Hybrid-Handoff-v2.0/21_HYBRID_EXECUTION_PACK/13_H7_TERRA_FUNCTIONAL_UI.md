# H7 — Terra / Medium — Functional Hybrid UI

> Execute only if H6R authorizes H7.

```text
重新读取 apple-design + 08 legacy design reference + Master PRD v2 + approved visual v1.9。

实现功能 UI：

Monitor-owned capable mode
Desktop-only neutral mode
mixed mode
stale/unavailable mode
capability-disabled settings

保持：
B capsule + white outline
Orb 90pt
single-click read-only Quick View
native Toggle/Slider/Picker
Usage/Settings titlebar behavior
no Hover

Current H1 baseline prohibited product capabilities remain hidden/disabled：
WAITING_APPROVAL
Session Token
live multi-thread
FAILED/INTERRUPTED real adapter projection
Desktop rows until classification promoted
reset consume

输出 PHASE_H7_REPORT.md。
停止。
```
