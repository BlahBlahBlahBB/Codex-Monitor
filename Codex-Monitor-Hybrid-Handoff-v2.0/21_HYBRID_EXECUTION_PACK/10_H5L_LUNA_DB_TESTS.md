# H5L — Luna / Medium — Bounded DB/Fixture Tests

> Run only after H5 architecture is accepted.

```text
只补：
migration tests
source namespace tests
snapshot/live separation
UPSERT tests
zero/null fixtures
provenance queries
capability history tests

不得改 schema/API。
问题只报告。

输出 PHASE_H5L_REPORT.md。
```
